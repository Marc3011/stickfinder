{
  "name": "hockey-stick-recommender",
  "version": "1.0.0",
  "description": "Hockey stick recommendation app with scraping and store directory",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "scrape": "node -e \"require('./scraper').runScrape().then(r => { console.log(JSON.stringify(r, null, 2)); process.exit(0); }).catch(e => { console.error(e); process.exit(1); })\""
  },
  "dependencies": {
    "axios": "^1.7.7",
    "cheerio": "^1.0.0",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "node-cron": "^3.0.3",
    "puppeteer": "^24.40.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT"
}
